apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki
spec:
  template:
    spec:
      containers:
        - name: loki
          volumeMounts:
            - name: data
              mountPath: /var/lib/loki
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: loki-pvc